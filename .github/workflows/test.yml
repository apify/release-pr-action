# For more info see: https://github.com/apify/pull-request-toolkit-action/
name: Test

on:
  workflow_dispatch:

  push:
    branches:
      - cc/test

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: run
        # run: 'dist/index.js'
        run: "echo TEST"
